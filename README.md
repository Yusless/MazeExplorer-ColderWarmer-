# MazeExplorer-ColderWarmer-

Сборка с нуля:

rm -rf build
mkdir build
cd build
cmake .. -G "MinGW Makefiles"
cmake --build .

Быстрая пересборка:

cmake --build .

Требует установленного git и cmake
